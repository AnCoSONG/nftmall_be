export enum SupportPayment {
  WX = 'weixin',
  ALI = 'alipay',
}

export enum SupportType {
  IMAGE = 'image',
  VIDEO = 'video',
  AUDIO = 'audio',
  D3 = '3D',
  HYBRID = 'hybrid',
}

export enum DisplayMode {
  LIGHT = 'light',
  DARK = 'dark',
}
